[project]
name = "trig-seq"
description = "MIDI DJ deck sequencer — fixed playhead, scrolling piano roll, two decks, crossfader"

[devlog]
version = "v1.2"
source = "https://github.com/dnewcome/devlog-tools"
